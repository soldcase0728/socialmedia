# Wilson-Rode derived index -- Python dependencies
#
# Everything here runs locally. No package in this list sends document
# content anywhere.
#
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt

PyMuPDF>=1.24,<2          # PDF page count, text extraction, image metrics
pypdf>=4.2,<8             # fallback reader for files MuPDF rejects
pandas>=2.2,<4            # tabular helpers
openpyxl>=3.1,<4          # Excel workbooks with freeze panes and filters
python-dateutil>=2.9,<3   # permissive date fallback parsing
rapidfuzz>=3.9,<4         # near-duplicate similarity confirmation
tqdm>=4.66,<5             # progress bars
Pillow>=10.3              # image handling for OCR paths
PyYAML>=6.0,<7            # configuration

# Development / testing
pytest>=8.2,<9
